<canvas wx:if="{{ !isUseNew }}" canvas-id="{{ canvasId }}"
  class="ec-canvas"
  bindtouchstart="{{ ec.disableTouch ? '' : 'touchStart' }}"
  bindtouchmove="{{ ec.disableTouch ? '' : 'touchMove' }}"
  bindtouchend="{{ ec.disableTouch ? '' : 'touchEnd' }}">
</canvas>
<canvas wx:else type="2d" 
  class="ec-canvas"
  canvas-id="{{ canvasId }}"
  bindtouchstart="{{ ec.disableTouch ? '' : 'touchStart' }}"
  bindtouchmove="{{ ec.disableTouch ? '' : 'touchMove' }}"
  bindtouchend="{{ ec.disableTouch ? '' : 'touchEnd' }}">
</canvas> 